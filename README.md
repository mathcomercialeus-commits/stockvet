# Vet Stock Control

Sistema interno de controle de estoque unico para clinica veterinaria.

As credenciais administrativas devem ser configuradas no ambiente do servidor com `ADMIN_LOGIN` e `ADMIN_PASSWORD` ou `ADMIN_PASSWORD_HASH`. Nao salve senhas no repositorio.
